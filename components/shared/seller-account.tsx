'use client'
// Apple Avenue is single-merchant (admin-only).
// Multi-seller marketplace CTA is disabled by spec: "ONLY HAS 1 MERCHANT WHICH IS THE ADMIN".
// Component intentionally renders nothing. Keep file for import compatibility.
const SellerAccount = () => null
export default SellerAccount
